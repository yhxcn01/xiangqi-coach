# 会话导出 — 象棋思考教练（Rust 实现）

- 导出时间：2026-09-08 23:13
- 工作目录：`D:\test\xiangqi`
- 会话主题：参考公众号文章思路，用 Rust 实现象棋教学软件「象棋思考教练」

---

## 用户请求（2026-07-30 23:56）

> https://mp.weixin.qq.com/s/7iyd7R7Y4nueehH_IxncDw，参考这个链接的介绍和思路，用 Rust 实现一个象棋教学软件，叫 [象棋思考教练]

## 参考文章要点（已抓取原文）

文章《我用 Rust 写了个象棋教学软件，被群友直接封神》的核心思路：

1. **痛点**：传统象棋软件只给"+180 红方稍优"这类数字，爱好者看不懂、不知道下一步该走啥、为什么这么走。
2. **产品理念**：**引擎是大脑，AI 是嘴。大脑算对，嘴讲明白。**
   - 第一层保险：专业引擎（原文用 pikafish）算出确定性最优走法；
   - 第二层：AI 只负责把引擎结论翻译成大白话，不参与计算；
   - 第三层：AI 服务不可用时自动降级为基础提示——宁可不讲，也不乱讲。
3. **两种讲解模式**：
   - 「🧑‍🏫 AI 讲解当前局面」：讲对方上一步意图、留下的弱点、你该怎么走、哪些坑不能踩；
   - 「AI 讲解这枚棋」：选中棋子后讲这枚棋的最佳走法、理由，以及差走法各亏多少分。
4. **AI 是加分项不是门槛**：不配 Key 也能完整下棋，只是讲解简短。

## 实现过程

### 环境准备

- 本机无 Rust 工具链 → 通过 rustup 安装 stable（x86_64-pc-windows-gnu，minimal），最终 cargo/rustc 1.97.1。
- 编译时缺 `dlltool.exe` → 从清华 MSYS2 镜像下载 `mingw-w64-x86_64-binutils-2.47-3`，用 Python zstandard 解压，将 `as.exe / ar.exe / ranlib.exe / nm.exe / objcopy.exe / objdump.exe / strip.exe / windres.exe` 复制进 rustup self-contained 目录解决。
- 构建 PATH 需包含：`~/.cargo/bin` 和 self-contained 目录。

### 项目结构

```
D:\test\xiangqi
├── Cargo.toml            # axum 0.7 / tokio / serde / reqwest(native-tls) / tower-http
├── package.json          # npm run dev → cargo run --
├── settings.json         # AI 服务配置（运行后生成）
├── src
│   ├── main.rs           # axum Web 服务 + REST API（默认端口 7100，支持 --port）
│   ├── xiangqi.rs        # 棋盘表示(90格)、全部走法规则、将军/绝杀/困毙判定、中文记谱法
│   ├── engine.rs         # α-β 搜索 + 吃子静态搜索、子力+位置评估、候选走法全评分
│   └── coach.rs          # AI 讲解层：LLM 调用(OpenAI 兼容) + 无 Key 自动降级模板
└── static
    └── index.html        # Canvas 棋盘、点击走棋、讲解面板(打字机效果)、设置弹窗
```

### API 一览

| 接口 | 说明 |
|---|---|
| `GET /api/state` | 当前棋盘、轮次、历史记谱、是否将军/终局、是否已配 AI |
| `POST /api/move` | 玩家走棋 `{from,to}`（服务端校验合法性） |
| `POST /api/engine_move` | 引擎（黑方）应着 |
| `POST /api/undo` | 悔棋（玩家+引擎两步） |
| `POST /api/new` | 新对局 |
| `GET /api/legal/:sq` | 指定棋子的合法目标格 |
| `POST /api/explain/position` | 讲解当前局面（AI 或降级模板） |
| `POST /api/explain/piece` | 讲解选中棋子 `{square}` |
| `GET/POST /api/settings` | AI 服务配置（base_url / api_key / model） |

### 端到端验证结果（全部通过）

- 初始局面 FEN/数组正确；`炮(64)` 合法目标含隔子打马(1)等，规则正确。
- 走 64→67 生成记谱 **「炮八平五」**（中文记谱正确，含红方从右数列号）。
- 引擎 0.5s 应出 **「砲8平5」**；局面讲解正确推荐 **「炮五进四：吃掉对方的卒，叫将」**。
- 单棋讲解：红马最佳 **「马八进七：出动大子」**，并标注 **「马八进九亏约 82 分」**（边马臭棋识别正确）。
- 悔棋、新对局、首页加载（HTTP 200）正常；测试后服务器已停止。

## 最终交付说明

- 预览：`npm run dev`（即 `cargo run`），打开 http://localhost:7100/
- 搜索深度：debug 构建 3 层 / release 构建 4 层（吃子静态搜索最深 8 层）
- 未配 AI Key 时所有讲解走引擎降级模板；配置 DeepSeek/GLM 后输出 120~220 字大白话棋理
- 遗留说明：若执行 `cargo clean` 后重编译，需要 PATH 中包含 rustup self-contained 目录（dlltool/as）

## 后续可做（已提议，待确认）

- **复盘模式**：一盘结束后逐回合标出关键失误（哪步亏了多少分、该走什么）。
